# 任务摘要：图片压缩 + 分类排序功能

## 时间
2026-05-11 17:03 - 17:28

## 目标
1. 上传图片自动压缩，生成缩略图 + 原图
2. 分类支持拖拽排序

## 已完成

### 代码修改
1. **安装 sharp 库** - Node.js 图片处理库
2. **修改上传 API** (`/api/upload/route.ts`)
   - 上传时自动生成两个版本：
     - 原图：1920px 宽，WebP 格式，质量 85%
     - 缩略图：800px 宽，WebP 格式，质量 80%
3. **创建分类排序 API** (`/api/categories/reorder/route.ts`)
   - 支持批量更新分类 sortOrder
4. **修改后台页面** (`/admin/page.tsx`)
   - 添加分类拖拽排序功能（拖拽手柄 + 拖拽事件）
   - 批量上传使用缩略图
   - 作品列表显示缩略图
5. **修改瀑布流组件** (`GallerySection.tsx`)
   - 瀑布流显示缩略图
   - 灯箱/弹窗显示原图

### Git 操作
- Commit: `d31e647` - "feat: auto image compression + thumbnail + category drag sort"
- 已推送到 GitHub

## 待用户操作

### 1. 数据库迁移（必需）
在 Supabase SQL Editor 执行：
```sql
ALTER TABLE "Work" ADD COLUMN IF NOT EXISTS "thumbnailUrl" TEXT;
UPDATE "Work" SET "thumbnailUrl" = "imageUrl" WHERE "thumbnailUrl" IS NULL;
```

### 2. 删除旧数据
- 登录后台 `/admin`
- 删除所有分类和作品

### 3. 重新创建
- 创建分类（可以拖拽排序）
- 重新上传作品（自动压缩生效）

## 功能说明

### 图片处理
- 新上传的图片自动压缩
- 瀑布流加载快（缩略图 ~100KB）
- 点击放大清晰（原图 ~300-500KB）
- 旧图兼容（无缩略图时显示原图）

### 分类排序
- 后台分类标签页支持拖拽
- 拖拽手柄（6个点图标）
- 拖动调整顺序自动保存
- 首页按排序显示

## Railway 部署
- 代码已推送，Railway 会自动部署
- 部署完成后即可使用新功能
